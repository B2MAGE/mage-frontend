diff --git a/node_modules/mage-local/dist/mage-engine.js b/node_modules/mage-local/dist/mage-engine.js
index 9630baf..88fa328 100644
--- a/node_modules/mage-local/dist/mage-engine.js
+++ b/node_modules/mage-local/dist/mage-engine.js
@@ -71805,7 +71805,95 @@ var require_shader_park_core_umd = /* @__PURE__ */ __commonJSMin(((exports, modu
 			].map(function(el) {
 				return el.toString();
 			}).join("\n"));
-			eval(generatedJSFuncsSource + postGeneratedFunctions + userProvidedSrc);
+			var evalGlobals = {
+				PI,
+				TWO_PI,
+				TAU,
+				time,
+				mouse,
+				normal,
+				mouseIntersection,
+				getRayDirection,
+				mixMat,
+				resetMixColor,
+				union,
+				difference,
+				intersect,
+				blend,
+				mixGeo,
+				overwrite,
+				shape,
+				mult,
+				add,
+				sub,
+				divide,
+				setSDF,
+				getSDF,
+				extractSDF,
+				reset,
+				displace,
+				getSpace,
+				setSpace,
+				repeat,
+				rotateX,
+				rotateY,
+				rotateZ,
+				mirrorX,
+				mirrorY,
+				mirrorZ,
+				mirrorXYZ,
+				flipX,
+				flipY,
+				flipZ,
+				expand,
+				shell,
+				color,
+				reflectiveColor,
+				metal,
+				shine,
+				fresnel,
+				lightDirection,
+				backgroundColor,
+				noLighting,
+				basicLighting,
+				occlusion,
+				test,
+				input,
+				input2: input2D,
+				input2D,
+				setStepSize,
+				setGeometryQuality,
+				setMaxIterations,
+				setMaxReflections,
+				getPixelCoord,
+				getResolution,
+				get2DCoords,
+				enable2D,
+				revolve2D,
+				extrude2D,
+				getSpherical,
+				mirrorN,
+				grid,
+				torus,
+				cylinder,
+				repeatLinear,
+				repeatRadial,
+				scaleShape,
+				vectorContourNoise
+			};
+			var previousEvalGlobals = {};
+			Object.keys(evalGlobals).forEach(function(key) {
+				previousEvalGlobals[key] = globalThis[key];
+				globalThis[key] = evalGlobals[key];
+			});
+			try {
+				eval(generatedJSFuncsSource + postGeneratedFunctions + userProvidedSrc);
+			} finally {
+				Object.keys(evalGlobals).forEach(function(key) {
+					if (previousEvalGlobals[key] === void 0) delete globalThis[key];
+					else globalThis[key] = previousEvalGlobals[key];
+				});
+			}
 			if (enable2DFlag) setSDF(0);
 			var geoFinal = userGLSL + "\n" + buildGeoSource(geoSrc);
 			var colorFinal = buildColorSource(colorSrc, useLighting);
@@ -112701,6 +112789,7 @@ var MAGEEngine = class MAGEEngine {
 		fadeMs: 700
 	};
 	#_pendingSkyboxLoad = null;
+	#animationFrameId = null;
 	constructor({ canvas, log = false } = {}) {
 		if (log) console.log(`Initializing MAGE Engine v${MAGE_VERSION}...`);
 		this.#canvas = canvas || null;
@@ -112766,6 +112855,7 @@ var MAGEEngine = class MAGEEngine {
 			fadeMs: 700
 		};
 		this.#_pendingSkyboxLoad = null;
+		this.#animationFrameId = null;
 	}
 	/**
 	* Initializes the MAGE Engine, creating the Three.js scene, camera, renderer, and other core components.
@@ -112777,7 +112867,7 @@ var MAGEEngine = class MAGEEngine {
 			this.#composer = effects.applyPostProcessing(this.#scene, this.#renderer, this.#camera);
 			this.#_syncSobelResolution();
 		}
-		this.#_render();
+		if (this.#animationFrameId === null) this.#animationFrameId = requestAnimationFrame(this.#_render);
 		if (!this.#currentPreset && !this.#visualizer.mesh) this.#_loadDefaultPreset();
 	}
 	/**
@@ -113135,6 +113225,10 @@ var MAGEEngine = class MAGEEngine {
 	* @returns {void}
 	*/
 	dispose() {
+		if (this.#animationFrameId !== null) {
+			cancelAnimationFrame(this.#animationFrameId);
+			this.#animationFrameId = null;
+		}
 		if (this.#renderer) {
 			this.#renderer.dispose();
 			this.#renderer.forceContextLoss();
@@ -113764,7 +113858,9 @@ var MAGEEngine = class MAGEEngine {
 		if (this.log) console.log("Visualizer Loaded!");
 	}
 	#_render = () => {
-		requestAnimationFrame(this.#_render);
+		this.#animationFrameId = null;
+		if (!this.#renderer || !this.#scene || !this.#camera || !this.#controls || !this.#clock || !this.#state || !this.#inputs) return;
+		this.#animationFrameId = requestAnimationFrame(this.#_render);
 		this.#_syncViewport();
 		const delta = this.#clock.getDelta();
 		this.#state.time = delta;
@@ -115423,4 +115519,4 @@ function initMAGE(options = {}) {
 //#endregion
 export { initMAGE };
 
-//# sourceMappingURL=mage-engine.js.map
\ No newline at end of file
+//# sourceMappingURL=mage-engine.js.map
